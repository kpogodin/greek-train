# Working agreements

- Push to `origin/main` without asking for confirmation first. Pushing triggers the deploy pipeline (`.github/workflows/deploy.yml`) — that's expected and fine, just do it once changes are ready and committed.
